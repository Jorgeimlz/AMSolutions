<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resultados de Enlaces de Redes Sociales</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/styles/styles.css">
</head>
<body class="bg-dark text-light">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Cuentas para <%= query %></h1>
        <ul class="list-group">
            <% cuentas.forEach(function(cuenta) { %>
                <li class="list-group-item bg-secondary text-light">
                    <%= cuenta.red %>: <a href="<%= cuenta.enlace %>" class="text-info"><%= cuenta.enlace %></a> - Riesgo: <span class="text-<%= cuenta.riesgo %>"><%= cuenta.riesgo %></span>
                </li>
            <% }) %>
        </ul>
        <div class="text-center mt-4">
            <a href="/" class="btn btn-primary">Volver a la Búsqueda</a>
        </div>
    </div>
</body>
</html>





